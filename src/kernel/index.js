import { EventBus } from './EventBus.js';\nimport { ModuleRegistry } from './ModuleRegistry.js';\nimport { PerceptionMemoryBridge } from '../proto/perception/PerceptionMemoryBridge.js';\n\nexport function createKernel() {\n    const bus = new EventBus();\n    const context = { bus, startedAt: Date.now(), version: '0.1.0' };\n    context.registry = new ModuleRegistry(context);\n    bus.onError((err, type) => console.error(`[kernel] event \\